import { FilterdepartmentPipe } from './filterdepartment.pipe';

describe('FilterdepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterdepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
